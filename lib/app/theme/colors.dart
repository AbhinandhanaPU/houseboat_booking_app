import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color lightPrimary = Color(0xff00B5CC);
  static const Color darkPrimary = Color(0xff005B66);

  // Common Colors
  static const Color whiteColor = Colors.white;
  static const Color blackColor = Colors.black;
  static const Color successColor = Colors.green;
  static const Color errorColor = Colors.red;

  // Background Colors
  static const Color backgroundPrimary = Color(0xff1B2230);
  static const Color backgroundSecondary = Color(0xff252E41);

  // Additional Colors
  static const Color lightGrey = Color(0xffB3B3B3);
  static const Color transparent = Colors.transparent;

  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.grey;
}
